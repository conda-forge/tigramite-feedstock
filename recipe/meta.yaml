{% set name = "tigramite" %}
{% set version = "5.2.9.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9b8109ef6dd3e26bbead06b78ae48924714863bf9831da8f4950465ce6e5cc2d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools
  run:
    - python >=3.6
    - numpy  >=1.18
    - scipy  >=1.10.0
    - six

test:
  imports:
    - tigramite
    - tigramite.independence_tests
    - tigramite.toymodels
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/jakobrunge/tigramite
  summary: 'Tigramite is a python package for causal inference with a focus on time series data'
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: license.txt
  doc_url: https://jakobrunge.github.io/tigramite
  dev_url: https://github.com/jakobrunge/tigramite

extra:
  recipe-maintainers:
    - martinRenou
